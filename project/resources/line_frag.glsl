#version 330 core
in vec3 fragPos;
out vec4 color;

//in vec3 light_I;
//uniform vec3 lightDir;

void main()
{
    //float intensity;
	//intensity = dot(lightDir ,normalize(fragNor));

    color = vec4(1,0,0,1);


}
